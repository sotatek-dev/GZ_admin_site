import { SaleRoundIcon } from 'src/assets/icons';
import Icon from '@ant-design/icons';

export const SiderIcon = {
	SaleRound: () => (
		<Icon component={() => <img src={SaleRoundIcon} />} alt='' />
	),
};
